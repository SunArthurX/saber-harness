[package]
name = "saber-resumption"
description = "Verifiable Resumption Capsules preserving goal, task, artifact and decision lineage with environment drift reconcile for Saber"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish = false

[dependencies]
saber-policy = { path = "../policy" }
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"

[dev-dependencies]
tempfile = "=3.27.0"

[lints]
workspace = true
