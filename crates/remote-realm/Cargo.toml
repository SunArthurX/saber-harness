[package]
name = "saber-remote-realm"
description = "Remote Execution Realm with traveling policy envelopes, heartbeat leases, digest-admitted results and cell-contained failures for Saber"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish = false

[dependencies]
saber-egress = { path = "../egress" }
saber-orchestrator = { path = "../orchestrator" }
saber-policy = { path = "../policy" }
saber-sandbox = { path = "../sandbox" }
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
sha2 = "=0.10.9"

[lints]
workspace = true
