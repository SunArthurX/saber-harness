[package]
name = "saber-context-engine"
description = "Permission-aware context engine and knowledge mesh with nutrition labels, scope isolation and rebuildable hybrid retrieval for Saber"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish = false

[dependencies]
saber-egress = { path = "../egress" }
saber-policy = { path = "../policy" }
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
sha2 = "=0.11.0"

[lints]
workspace = true
