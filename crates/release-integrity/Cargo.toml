[package]
name = "saber-release-integrity"
description = "Verifiable release integrity: reproducible manifests, SBOM, provenance, signed target chains, staged rings and a verify-before-install updater for Saber"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish = false

[dependencies]
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
sha2 = "=0.11.0"

[lints]
workspace = true
