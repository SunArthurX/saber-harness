[package]
name = "saber-beta-slo"
description = "Design Partner Beta: deterministic SLO benchmark harness, budgets as contracts, opt-in metadata-only telemetry and candidate-only feedback intake for Saber"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish = false

[dependencies]
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"

[lints]
workspace = true
