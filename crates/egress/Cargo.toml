[package]
name = "saber-egress"
description = "Default-deny egress policy enforcement point with destination, taint, DLP, redirect and DNS-rebinding controls for Saber"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish = false

[dependencies]
saber-policy = { path = "../policy" }
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
sha2 = "=0.10.9"

[lints]
workspace = true
