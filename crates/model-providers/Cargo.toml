[package]
name = "saber-model-providers"
description = "Policy-bound replaceable model layer: provider SPI, adapters, registry, router and budgets for Saber"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish = false

[dependencies]
saber-egress = { path = "../egress" }
saber-policy = { path = "../policy" }
saber-sandbox = { path = "../sandbox" }
saber-secret-broker = { path = "../secret-broker" }
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
sha2 = "=0.10.9"

[lints]
workspace = true
