[package]
name = "saber-sandbox"
description = "Fail-closed sandbox SPI, execution realms and path/environment guards for Saber"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish = false

[dependencies]
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
sha2 = "=0.11.0"

[dev-dependencies]
tempfile = "=3.27.0"

[lints]
workspace = true
