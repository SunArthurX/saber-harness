# Canonical schemas

S03 will add versioned JSON Schema 2020-12 contracts for domains, events, capabilities and the local control protocol. Rust and TypeScript types are generated; handwritten duplicate wire types are forbidden by ADR-006.
