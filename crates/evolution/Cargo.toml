[package]
name = "saber-evolution"
description = "Governed self-evolution workshop: typed candidate lifecycle, evaluation records, explicit review promotion and revocation for Saber"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish = false

[dependencies]
saber-memory-authority = { path = "../memory-authority" }
saber-policy = { path = "../policy" }
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"

[lints]
workspace = true
