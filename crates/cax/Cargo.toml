[package]
name = "saber-cax"
description = "Canonical Agent Exchange records with recomputable evidence, first importers and an idempotent revocable import library for Saber"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish = false

[dependencies]
saber-context-engine = { path = "../context-engine" }
saber-policy = { path = "../policy" }
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
sha2 = "=0.11.0"

[lints]
workspace = true
