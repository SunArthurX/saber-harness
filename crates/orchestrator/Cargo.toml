[package]
name = "saber-orchestrator"
description = "Typed Goal DAG, attenuated subagent delegation, evidence judgment and isolated failure domains for Saber"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license-file.workspace = true
publish = false

[dependencies]
saber-policy = { path = "../policy" }
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"

[lints]
workspace = true
